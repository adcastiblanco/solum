Issue 01 — Project bootstrap — DONE (2026-05-19)

- Next.js 16 (App Router, TS, Tailwind v4, ESLint) scaffolded into repo root via tempdir + copy, preserving .git, .scratch/, docs/, supabase/, files/, afk-ralph.sh, CLAUDE.md, .env.local, .env.local.example, .gitignore.
- Deps installed: @mistralai/mistralai, @supabase/supabase-js, @supabase/ssr, geist, react-pdf.
- .env.local.example lists NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, MISTRAL_API_KEY. .env.local is gitignored.
- supabase/migrations/0001_init.sql already in place with documents (incl. error_message), extractions, field_reviews, RLS policies, and the documents storage bucket + policies.
- Design tokens (navy palette, neutrals, green-50/700, canvas, radii, font vars) wired into app/globals.css as CSS custom properties + Tailwind @theme inline mappings.
- Fonts: Geist + Geist Mono via the geist npm package; Instrument Serif via next/font/google. CSS vars: --font-geist-sans, --font-geist-mono, --font-instrument-serif.
- app/page.tsx renders a canvas-background landing page with the Solum Health title in Instrument Serif italic navy. npm run dev boots cleanly on :3000 (HTTP 200).

Next: Issue 02.

Issue 02 — Auth shell + empty dashboard — DONE (2026-05-19)

- Supabase SSR wired with helpers: lib/supabase/client.ts (createBrowserClient), lib/supabase/server.ts (createServerClient over cookies()), and lib/supabase/middleware.ts (createServerClient over request cookies, refreshes session and gates routes).
- middleware.ts at repo root delegates to updateSession. Public paths: /login, /signup, /auth/*. Authenticated users hitting /login or /signup are bounced to /dashboard; unauthenticated users on any other route get redirected to /login.
- Auth routes under app/(auth)/: /login and /signup share auth-form.tsx and a centered layout. Server actions in login/actions.ts and signup/actions.ts call signInWithPassword / signUp + immediate sign-in, then revalidate and redirect to /dashboard. Errors surface via ?error= query param.
- Protected shell under app/(app)/: layout fetches the user (redirects to /login if absent) and renders Nav + canvas main. Dashboard route renders the navy serif header and a dashed empty-state card (no upload yet).
- components/nav.tsx is the persistent nav: navy background, Instrument Serif logo, Dashboard + Accuracy links, user email, and a Log out form that posts to the signOut server action in app/auth/actions.ts (signs out, revalidates, redirects to /login).
- Root / now redirect()s to /dashboard so the middleware handles the auth gate uniformly.
- `npx next build` passes; the only warning is the Next 16 "middleware → proxy" deprecation notice (cosmetic for MVP).

Next: Issue 03.

Issue 03 — Upload + dashboard list — DONE (2026-05-19)

- Dashboard page (app/(app)/dashboard/page.tsx) is now a server component that fetches the authenticated user's documents (RLS-scoped) and renders a header with the Uploader on the right and the document list below.
- components inside the dashboard route: uploader.tsx (client) and document-list.tsx (server). Uploader uses a hidden <input type="file" accept="application/pdf" multiple/> behind a navy "Upload PDFs" button.
- Upload flow per file: generate a UUID client-side, insert a documents row with status='pending' and storage_path={user_id}/{document_id}-{filename}, router.refresh() so the row appears immediately, then upload to the 'documents' Supabase Storage bucket. Failed uploads mark the row status='error' with error_message.
- Document list renders filename (links to /review/{id}, which 404s for now), navy status badge (navy-light/navy for pending|processing|done, neutral border + hover-tooltip for error), and a formatted created_at timestamp.
- Uploading the same filename twice yields two rows with distinct storage paths because the document UUID is embedded in the path.
- `npx next build` passes (only the pre-existing middleware → proxy deprecation warning).

Next: Issue 04.

Issue 04 — Mistral extraction + status polling — DONE (2026-05-19)

- lib/types.ts now defines the clinical FIELD_LABELS map (snake_case → human label), FIELD_NAMES order, ARRAY_FIELDS set, and ExtractedField/BBox types shared by extractor + future review UI.
- lib/mistral.ts implements MistralExtractor: builds a `{ value, confidence, bbox: { page, x, y, width, height } }` JSON schema per field (string vs string[] per ARRAY_FIELDS), calls client.ocr.process with model 'mistral-ocr-2505' + documentAnnotationFormat (json_schema) + documentAnnotationPrompt, parses the JSON annotation, and normalizes every field — including ones the model omitted — into ExtractedField[]. Errors are mapped to MistralExtractionError with kinds timeout|transport|schema|unknown so the API route can surface readable messages.
- app/api/extract/route.ts (POST { documentId }) authenticates via SSR Supabase, sets status='processing'+clears error_message, signs a 10-minute storage URL, calls extractDocument, inserts a row in extractions with raw_mistral_response + extracted_fields, then flips status='done'. On any failure it writes status='error' with a user-facing error_message and 500s — the same code path is reused by the Retry button (future issue) since it just re-POSTs the documentId.
- app/api/documents/route.ts (GET) returns the authenticated user's documents (RLS-scoped, newest first) for the poller.
- hooks/useDocumentPolling.ts polls /api/documents every 2.5s while any doc is pending|processing, stops when none are; survives refresh because the effect re-evaluates whenever a doc's status changes. Exposes refresh() so the uploader can force-include a freshly-inserted row.
- Dashboard restructured: page.tsx still server-renders the initial list, but rendering is delegated to app/(app)/dashboard/dashboard-client.tsx, a client component that owns polling state and passes refresh to <Uploader onChange={refresh}/>.
- uploader.tsx now calls onChange() right after inserting the documents row (so the pending row appears immediately), then uploads to Storage, then fires POST /api/extract (fire-and-forget — the route flips status, polling picks it up). Storage upload failures still write status='error' inline.
- MISTRAL_API_KEY is read inside lib/mistral.ts via process.env on the server only; never referenced under NEXT_PUBLIC_.
- `npx next build` + `npx eslint` both pass cleanly.

Next: Issue 05.

Issue 05 — Review screen: PDF viewer + form sidebar — DONE (2026-05-19)

- New route app/(app)/review/[documentId]/page.tsx is a server component: fetches the document (RLS scoped, notFound() if missing/not-owned), the latest extraction row, and a 30-minute signed URL from the 'documents' Storage bucket. Normalizes extracted_fields against FIELD_NAMES so every schema field appears (missing ones get value=null) before handing to the client.
- review-client.tsx owns the 60/40 split layout inside a --r-lg card on --canvas, with a Dashboard back-link, the file name in Instrument Serif italic navy, and a status tag. The PDF viewer is dynamically imported with ssr:false so react-pdf only runs on the client.
- pdf-viewer.tsx wires pdfjs.GlobalWorkerOptions.workerSrc to `new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url)` (works through Turbopack), imports the AnnotationLayer/TextLayer CSS, and renders all pages stacked in a scroll container with a ResizeObserver-driven Page width so multi-page PDFs are fully reachable. cMapUrl pinned to the pdfjs-dist@5.4.296 unpkg path.
- field-card.tsx renders the four-state visual treatment (Neutral/Hovered/Missing; Approved unused this slice): white+gray border by default, navy-light bg + navy border on hover, gray-tinted bg with italic "Not detected" placeholder for missing values. Label in Geist, snake_case field name tag in Geist Mono. Inline editable input for scalar fields; ARRAY_FIELDS get per-item inputs with an "+ add" affordance.
- Edits are held in local React state only (no persistence yet — that's slice 07). Hover state is tracked in the client wrapper so slice 06 can drive the bbox overlay from the same `hovered` value. No emojis anywhere.
- `npx next build` + `npx eslint` both pass cleanly.

Next: Issue 06.

Issue 06 — BBox highlight + confidence on hover — DONE (2026-05-19)

- New pure-render module components/BBoxOverlay.tsx: given a normalized BBox + confidence, positions an absolutely-placed highlight (navy border, rgba(30,58,95,0.08) fill, --r-sm radius) and a confidence tag (navy bg, white Geist Mono text, top-right of the highlight). Coordinate math is just `${x*100}%` / `${y*100}%` / etc., so it stays correct at any PDF zoom level because the overlay's parent is the page wrapper which scales with the Page width.
- pdf-viewer.tsx now accepts a `highlight: { bbox, confidence } | null` prop and exports the Highlight type. Each rendered page wrapper is registered into a `Map<pageNumber, HTMLDivElement>` ref; when `highlight` changes, an effect calls scrollIntoView() on the matching page so the viewer auto-jumps to off-page bboxes. The BBoxOverlay is rendered inside a `relative` wrapper around the matching <Page>, so it positions inside the actual rendered page bounds.
- review-client.tsx derives `highlight` via useMemo from the hovered field name + the fields array (passes confidence + bbox through). Missing fields (bbox === null) yield highlight=null, so hovering them shows nothing — matches the acceptance criterion.
- Confidence is rounded to whole percent (Math.round(c*100) → "94%") and only appears when the field has a numeric confidence; absence hides the tag entirely.
- `npx next build` passes with only the pre-existing middleware → proxy deprecation warning.

Next: Issue 07.

Issue 07 — Field approval + edit tracking — DONE (2026-05-19)

- New deep module lib/field-reviews.ts exports approveField(supabase, { extractionId, fieldName, originalValue, finalValue, confidence, bbox }). Lazy-creates a field_reviews row on first approval; on subsequent calls updates final_value, monotonically advances was_edited (true if existing.was_edited OR final !== original_value), and never overwrites original_value/confidence. Values are JSON-encoded via serializeValue/deserializeValue so arrays and scalars round-trip through the text columns uniformly.
- New API route app/api/review/route.ts (POST) authenticates via SSR Supabase, validates extractionId+fieldName, and delegates to approveField. RLS on field_reviews enforces per-user isolation through the documents→extractions join.
- field-card.tsx grew a 28×28 circular CheckButton with three visual states (default gray outline, hover green outline, approved filled green-700) plus a checkmark SVG. Approved cards switch to green-50 bg + green-700 border. The button is disabled for missing fields unless the field is already approved.
- review-client.tsx now owns approved/approving maps. handleApprove POSTs to /api/review with the snapshotted originalValue + current finalValue + confidence + bbox; on success flips the card to approved. Editing an approved value drops it back to neutral so the user must re-click — and the next approval picks up was_edited=true via the service.
- Review page server-component now also reads field_reviews for the latest extraction, deserializes final_value, and seeds the client's approved map + values so refresh restores prior approvals. Idempotency comes from the (extraction_id, field_name) unique index plus the monotonic was_edited rule.
- Green only appears on approved field cards and the check button; the Accuracy view stays navy-only (deferred to slice 09).
- `npx next build` passes (only the pre-existing middleware → proxy deprecation warning).

Next: Issue 08.

Issue 08 — Failure handling + Retry — DONE (2026-05-19)

- app/api/extract/route.ts now maps catches into short user-facing strings via a `toUserMessage` helper: `MistralExtractionError.kind` → "Mistral OCR timed out" (timeout), "Unsupported document format" (schema), "Document could not be read" (transport); everything else → "Extraction failed — try again". The mapped string is written verbatim into documents.error_message and returned in the 500 body. Successful runs continue to clear error_message back to null.
- document-list.tsx converted to a client component so the row can host a Retry button. The Error badge now uses navy text on a gray-bordered white chip with a native `title` tooltip showing the stored error_message (or a generic fallback). The badge is `cursor-help` to advertise the tooltip.
- New RetryButton (also in document-list.tsx) POSTs `/api/extract` with the failed documentId, shows a "Retrying" label while in-flight, and calls the dashboard's `refresh()` after the request settles so the row picks up the new status (processing → done|error) via the same polling path used by initial uploads.
- DashboardClient passes `onRetry={refresh}` into <DocumentList/>; the poller already runs whenever any doc is processing, so retries re-enter the normal state machine with no extra wiring.
- No automatic server-side retry — retry is strictly user-initiated through the badge button. Same code path as the initial extraction, so error → success → green list state, or error → error with an updated message, both work.
- `npx next build` passes (only the pre-existing middleware → proxy deprecation warning).

Next: Issue 09.
